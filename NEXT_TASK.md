# Next Task

Create branch:

`phase/01-foundation-discovery`

Then:

1. make bootstrap build green locally and in GitHub Actions,
2. replace bootstrap Gradle downloader with official wrapper,
3. implement `AndroidCameraCatalog`,
4. build normalized `CameraDeviceProfile`,
5. build logical/physical graph,
6. implement `ValuableCameraResolver`,
7. create Lens Diagnostics UI,
8. export sanitized JSON,
9. add unit tests using fake device graphs,
10. update `PROJECT_STATE.md`.

Do not start computational photography until this passes on physical hardware.
